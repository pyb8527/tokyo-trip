# Cloudflare 터널 토큰
#   Zero Trust 대시보드 → Networks → Tunnels → 터널 생성 시 발급되는 값
#   "eyJhIjoi..." 로 시작하는 긴 문자열입니다.
#
# 사용법:
#   cp .env.example .env
#   vi .env                      # 토큰 붙여넣기
#   docker compose --profile tunnel up -d
#
# ⚠️ .env 는 .gitignore 에 걸려 있습니다. 절대 커밋하지 마세요.
#    이 토큰이 있으면 남이 내 터널에 붙을 수 있습니다.

TUNNEL_TOKEN=여기에_터널_토큰

# ── API 서버 ─────────────────────────────────────────────────────────────
# 최초 관리자 계정을 만들 때 한 번만 쓰는 토큰입니다.
# 계정이 하나라도 생기면 /api/auth/setup 은 스스로 잠깁니다.
# 아무 긴 무작위 문자열로 두세요:  openssl rand -base64 32
SETUP_TOKEN=여기에_긴_무작위_문자열
